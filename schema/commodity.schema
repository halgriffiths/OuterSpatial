package commodity;

import "improbable/standard_library.schema";

type Commodity {
    string name = 1;
    // how much space 1 unit of the commodity takes up in inventory
    double size = 2;
    int32 component_id = 3;
}